name: patsynmusicmp3
description: Professional Offline MP3 Music Player
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  just_audio: ^0.9.36
  on_audio_query: ^2.9.0
  permission_handler: ^11.0.1
  shared_preferences: ^2.2.1
  audio_video_progress_bar: ^1.0.1
  flutter_audio_waveforms: ^1.2.3
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
